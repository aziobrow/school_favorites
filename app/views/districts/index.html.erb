<h1> All Denver Public Schools Districts </h1>

<ul>
  <% @districts.each do |district| %>
    <h3><li><%= link_to district.name, district_path(district) %> (<%= district.board_member %>)</li></h3>
  <% end %>
</ul>
